# render.yaml
services:
  - type: web
    name: parkinson-detector
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && apt-get update && apt-get install -y ffmpeg libopencore-amrnb
    startCommand: python app.py
    autoDeploy: true
